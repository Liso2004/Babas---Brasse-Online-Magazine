# Optional local-development settings.
# Copy this file to .env. Never commit the resulting .env file.

PORT=8787
HOST=127.0.0.1
BABAS_ADMIN_EMAIL=admin@example.com
BABAS_ADMIN_PASSWORD=change-this-local-password

# Local development uses apps/api/data/submissions.json automatically.
# DATABASE_URL and BABAS_WEB_DIST_PATH are not required for development.
